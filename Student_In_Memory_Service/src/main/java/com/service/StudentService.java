package com.service;

import java.util.ArrayList;
import java.util.List;

import org.springframework.stereotype.Service;

import com.model.Student;

// Marks this class as a service layer component
public class StudentService {

// In-memory list used as a student repository [ private List<Student> repo = new ArrayList<>(); ]

// Zero param Constructor to initialize students list 
	/* 
		repo.add(new Student(1, "Rahul", 21));
		repo.add(new Student(2, "Anita", 22));
	*/

// Method to get all students
// Return type: List<Student>

// Method to add a new student
// Return type: void

}
