package com.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RestController;

import com.model.Employee;


//mark this class as a REST controller

public class EmployeeController {

	//handle HTTP GET request at /get

	//method to return an Employee object (return type: Employee)

	//handle HTTP POST request at /add

	//method to accept Employee data from request body and return it (return type: Employee)

}